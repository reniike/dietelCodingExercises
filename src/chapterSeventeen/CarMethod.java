package chapterSeventeen;

public interface CarMethod {
    void stop();
    void move();
//    just one abstract method in A functional interface, you can have other static or default methods o, but
//    just one abstract method!
}
